int main(void)
{
    double lower = 99.5, upper = 99.6;
    int one = 1, two = 2, ninetynine = 99;
    if (lower > upper || ninetynine) {
        return 40 + (one < two || one < two) + (ninetynine || ninetynine);
    } else {
        return 0;
    }
}
